print(getwd())
print(list.files())
emp = read.csv('emp.csv')
print(dim(emp))
print(colnames(emp))
print(head(emp,2))
print(head(emp,3))
str(emp)
library(dplyr)
결과 = emp %>% filter(SAL >= 3000) %>% select(ENAME, JOB) 
print(결과)
결과 = emp %>% group_by(JOB) %>% summarise(AVG_SAL = mean(SAL), EMP_COUNT = n(), SUM_SAL = sum(SAL))
print(결과)
결과 = emp %>% filter(SAL >= 2000) %>%group_by(DEPTNO) %>%summarize(EMP_COUNT = n())
print(결과)
dept=read.csv('dept.csv')
str(dept)
#View(dept)
조인결과 = emp %>% inner_join(dept, by = "DEPTNO" )
조인결과= emp %>% inner_join(dept, by = "DEPTNO") %>% filter(LOC == 'DALLAS') %>%select(ENAME, JOB)
print(조인결과)
결과 = emp %>% slice(2, 4)
print(결과)
결과 = emp %>% slice(1:3)
print(결과)